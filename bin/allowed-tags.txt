# Controlled vocabulary for skill `tags:` frontmatter — the single source of truth.
# Both bin/link.sh (--tags filter) and bin/gen-readme-skills.sh (README grouping)
# read this file; an unknown tag on a skill or on the CLI is rejected, never
# silently ignored. Documented for authors in skills/skill-author/SKILL.md.
#
# Format: one tag per line, "<tag> <kind>". Blank lines and # comments ignored.
#   kind=domain  every skill carries exactly one of these (drives README grouping)
#   kind=topic   optional finer-grained facet, zero or more per skill

craft   domain
infra   domain
ops     domain

terraform      topic
observability  topic
networking     topic
integration    topic
access         topic
